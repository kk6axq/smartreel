# PlatformIO build artifacts -- regenerated by `pio run`.
.pio/

# VSCode generated metadata. PIO regenerates these on every open so
# there's no point versioning them; they'd just churn.
.vscode/

# Per-machine Claude Code session state.
.claude/

# macOS folder metadata.
.DS_Store
